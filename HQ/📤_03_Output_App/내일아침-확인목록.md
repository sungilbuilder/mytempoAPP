# 재확인 목록 (2026-08-01 수정본)

**Galaxy S25**에서 확인 · 어제 제보하신 3건 수정 완료

---

## 실행

```
cd "/Users/josungil/Desktop/claude projects/golf-tempo-app/HQ/📤_03_Output_App"
```

```
npm run go
```

> 패키지는 이미 설치돼 있으니 `npm run setup`은 생략해도 됩니다. 화면이 이상하면 그때 실행하세요.

---

## ✅ QA 선행 검증 완료 (창업자 확인 불필요)

이번부터는 코드로 확인 가능한 건 제가 먼저 걸렀습니다. 아래는 **이미 통과**한 항목이라 보실 필요 없습니다.

- `npm run typecheck` — **0 에러**
- `useEffect` 의존성에 매 렌더 새로 생기는 값 — **없음**
- 죽은 토글(눌러도 아무 일 없는 설정) — **없음**
- 사용 중인 expo-video API가 설치 버전(3.0.16)에 실제 존재하는지 — **전부 존재**
- import 상대경로 전수 해석 — **전부 해석됨**
- 화면 이동 대상이 실제 라우트인지 — **전부 존재**

---

## 확인해주실 것 — 코드로는 알 수 없는 것만

### 🔴 ① 어제 터진 3곳이 이제 되는지

원인은 **하나**였습니다 — 테마 동기화 코드가 무한 루프를 돌고 있었습니다.

- [ ] 홈에서 **바로 연습 시작** → 에러 없이 연습 화면 진입
- [ ] 설정 진입 → 에러 없음
- [ ] 설정에서 **테마 전환**(밝게/어둡게) → 즉시 바뀌고 에러 없음

> "글자 크기"는 없어졌습니다. 원래 눌러도 아무 변화가 없던 미완성 기능이라 뺐습니다. 필요하시면 제대로 만들어 넣겠습니다.

### 🔴 ② 온보딩 다시 보기

어제 온보딩이 안 뜬 건 정상 동작입니다 — 이미 완료한 걸로 저장돼 있어서요. 다만 설정이 에러가 나서 다시 볼 수가 없었습니다.

- [ ] **설정 → 앱 정보 → 온보딩 다시 보기** → 4장이 처음부터 나오는지
- [ ] 4번째 장에서 테마 선택 후 **무료로 시작하기** → 홈 진입

### 🔴 ③ 영상 확대 (새로 추가)

멀리서 찍은 영상 대응입니다.

- [ ] 마킹 화면 영상 **우측 하단에 1x / 1.5x / 2x / 3x** 버튼
- [ ] 2x 이상에서 **영상을 손가락으로 끌면 위치가 움직이는지**
- [ ] 1x로 돌아오면 위치가 원래대로 초기화되는지
- [ ] 확대한 상태에서도 프레임 버튼이 정상 동작하는지

### 🔴 ④ 슬로모 fps 인식 (수정됨)

어제는 갤러리에서 fps를 못 받아오는 문제가 있었습니다. **영상 트랙에서 직접 읽도록** 바꿨습니다.

- [ ] **슬로모로 찍은 영상** → 결과가 `3.2:1` 처럼 **소수점**까지 나오는지
- [ ] **일반 영상** → `약 3:1` 처럼 **정수**로 나오는지
- 슬로모인데 여전히 정수로 나오면 알려주세요

### 🟡 ⑤ 아직 확인 못 한 것 (어제 목록에서 이월)

- [ ] **메트로놈 루프 경계** — 1~2분 들으며 박자가 끊기거나 튀지 않는지 ← 여전히 최우선
- [ ] 앱 아이콘 / 스플래시 로고
- [ ] 한글이 단어 중간에서 잘리지 않는지
- [ ] 앱 완전 종료 후 재실행 시 저장 데이터 유지

---

## 이번에 고친 것

| 문제 | 원인 | 조치 |
|---|---|---|
| 여러 화면에서 무한 루프 에러 | 테마 동기화 `useEffect`가 매 렌더 새로 생기는 함수를 의존성으로 잡음 | 의존성 축소 + 탭 옵션 메모이즈 |
| 글자 크기 눌러도 변화 없음 | 애초에 구현되지 않은 죽은 토글 | v1에서 제거, 별도 과제로 분리 |
| — | expo-video의 존재하지 않는 API 사용 | 제거 (실동작엔 영향 없었음) |
| 슬로모인데 정수로 표시 | 갤러리가 fps를 안 줌 | 영상 트랙에서 직접 읽도록 변경 |
| 멀리서 찍으면 안 보임 | — | 확대 1~3x + 드래그 이동 추가 |
