# MYTEMPO — 에이전트 작업 규칙

## 문서는 이 저장소에 없다

기획·전략·브랜드·법무 문서는 전부 **옵시디언 볼트**로 이관됐다. 이 저장소에는 **코드와 에셋만** 남아 있다.

```
볼트 루트: ~/Documents/Obsidian Vault/MYTEMPO/
```

## 문서를 찾는 순서 (토큰 절약의 핵심)

1. **`MYTEMPO/00_INDEX.md` 하나만 먼저 읽는다.** 전 문서의 한 줄 설명 + "언제 열까"가 표로 정리돼 있다.
2. 필요한 파일의 **frontmatter `summary`(2~3줄)만** 확인한다. 대부분 여기서 답이 나온다.
3. 그래도 부족할 때만 본문을 연다.

**전문 검색(`grep -r` 볼트 전체)을 첫 수단으로 쓰지 말 것.** 인덱스 → summary → 본문 순서를 지킨다.

## 폴더 규칙

| 위치 | 성격 | 읽는 빈도 |
|---|---|---|
| `01_전략` ~ `09_조직` **직속 파일** | **결론·현행 문서(SSOT)**. 판단은 여기서 끝낸다 | 높음 |
| `_근거/`, `_기술/` 하위 | 그 결론에 이른 **원본 분석 문서**. 길다 | 낮음 — "왜 이렇게 정했나"를 물을 때만 |
| `09_조직/_기록/` | 부서 간 히스토리 로그(86KB) | 매우 낮음 — 이력 역추적 전용 |
| `99_Archive/` | **폐기·대체된 문서** | ⛔ 판단 근거로 쓰지 말 것 |

## frontmatter `status` 값

`확정` `진행중` `초안` `가설` `근거자료` `종결` `반영완료` `대체됨` `폐기` `이관완료`

`대체됨`·`폐기`·`이관완료`를 보면 **본문을 읽지 말고** summary에 적힌 대체 문서로 이동한다.

## 충돌 시 우선순위

1. `02_제품/v1.0-출시사양.md` (v1 관련 최우선)
2. `08_기록/결정사항-로그.md` · `01_전략/프로젝트-개요.md` · `05_지표/KPI-노스스타.md`
3. 그 외 카테고리 직속 파일
4. `_근거/` 하위 원본

⚠️ 자주 발생하는 오판: `99_Archive/수익화-v2-광고프리미엄.md`는 **광고 모델**을 전제하지만 확정안은 **광고 없음**이다.

## 문서를 갱신할 때

- 결론이 바뀌면 → 카테고리 직속 파일을 고치고, 이전 버전은 `99_Archive/`로 옮기며 `status: 폐기` + 대체 문서 링크를 남긴다.
- 새 분석 문서를 만들면 → `_근거/`에 넣고, 결론만 직속 파일에 반영한다. `_근거/` 문서는 직속 파일에서 링크한다.
- **어떤 문서를 만들든 frontmatter(`title`/`summary`/`tags`/`status`/`updated`)를 반드시 넣는다.** summary 없는 문서는 다음 세션에서 통째로 읽혀 토큰을 낭비한다.
- 지나간 작업 기록은 `09_조직/회사-메모리-현황.md`에 쌓지 말고 `09_조직/_기록/부서간-히스토리-로그.md`로 옮긴다.

## 보고 프로토콜

부서(서브에이전트) 소환 시 `09_조직/보고-프로토콜.md`를 따른다 — 자기 태그 `[@에이전트이름]` + 종료 시 `Daily_Report` 브리핑 + 창업자 확인 사안은 `Founder_Review = Y`.

## 코드 주석의 "PLANNING.md N절" 표기

앱 소스 주석 다수가 `PLANNING.md`를 참조한다. 이 파일은 볼트의
`02_제품/_근거/서비스기획서-PLANNING.md`로 이동했다. 절 번호는 그대로 유효하다.

## 저장소에 남아 있는 것

- `HQ/📤_03_Output_App/` — 앱 소스 (Expo SDK 54)
- `HQ/📑_02_Workspace/product_design/brand-system-v2/svg-final/` — **확정 로고 SVG**
- `HQ/📑_02_Workspace/product_design/brand-icons/store-assets/` — 스토어 아이콘 (최신)
- `HQ/📑_02_Workspace/strategy/*.xlsx` — WBS/Daily_Report, D-30 체크리스트
- `.claude/agents/` — 서브에이전트 정의 16개
